#!/usr/bin/env bash
#
# extract-traefik-certs <acme_json_path> <dest_dir>
# Extracts Traefik v3 acme.json certificates into fullchain.pem and privkey.pem
# uses ldez/traefik-certs-dumper via Docker

set -euo pipefail
IFS=$'\n\t'

action_metadata() {
  cat <<EOF
description: "Extracts Traefik v3 acme.json certificates into fullchain.pem and privkey.pem"
args:
  - SOURCE_ACME: "path to acme.json"
  - DEST_DIR: "destination directory"
EOF
}

SCRIPT_NAME="extract-traefik-certs"

# --- Parse arguments ---
SOURCE_ACME="${1:-}"
DEST_DIR="${2:-}"

require_arg "SOURCE_ACME" "$SOURCE_ACME" "Usage: $0 <path_to_acme.json> <destination_directory>"
require_arg "DEST_DIR" "$DEST_DIR" "Usage: $0 <path_to_acme.json> <destination_directory>"

# --- Validation ---
if [[ ! -f "$SOURCE_ACME" ]]; then
    log_error "Source file not found: $SOURCE_ACME"
    exit 1
fi

# Convert to absolute paths for Docker mounting
ABS_SOURCE="$(realpath "$SOURCE_ACME")"
SOURCE_DIR="$(dirname "$ABS_SOURCE")"
SOURCE_FILE="$(basename "$ABS_SOURCE")"

mkdir -p "$DEST_DIR"
ABS_DEST="$(realpath "$DEST_DIR")"

log_info "Extracting certificates..."
log_info "  Source: $ABS_SOURCE"
log_info "  Destination: $ABS_DEST"

# --- Execute Dumper via Docker ---
# We mount the source directory and the destination directory
# Traefik v3 requires the --version v3 flag
docker run --rm \
    -v "$SOURCE_DIR:/data" \
    -v "$ABS_DEST:/output" \
    ldez/traefik-certs-dumper:v2.9.3 \
    file --version v3 \
    --source "/data/$SOURCE_FILE" \
    --dest /output \
    --domain-subdir=true \
    --crt-name=fullchain \
    --crt-ext=.pem \
    --key-name=privkey \
    --key-ext=.pem

# --- Fix Permissions ---
# Docker creates files as root; we want them accessible but secure.
# If you are running this as part of your noroot setup, 
# you might want to chown these to your specific user.
log_info "Setting secure permissions (600) on extracted keys..."
find "$ABS_DEST" -type f -name "*.pem" -exec chmod 600 {} +

log_success "✅ Certificates extracted successfully!"
log_info "Structure created at $DEST_DIR:"
log_info "  └── [domain]/"
log_info "      ├── fullchain.pem"
log_info "      └── privkey.pem"
